[project]
name = "bash-cloud-forge"
version = "1.1.0"
description = "Pure Bash Cloud Provisioning API"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "sqlalchemy",
    "psycopg2-binary",
]

[tool.uv]
managed = true
